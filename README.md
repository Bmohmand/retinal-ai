# retinal-ai

Git LFS is configured to track everything under data/. Before pushing large assets, ensure LFS is installed:

1) Install Git LFS: https://git-lfs.com
2) Run once per machine: `git lfs install`
3) Add data locally (kept out of gitignore) and commit normally; LFS handles storage for data/
